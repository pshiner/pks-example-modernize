spring:
  datasource:
    url: jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1
    username: sa
    password: sa
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    open-in-view: true

# logging:

  # liquibase:
  #   change-log: classpath:/db/changelog/changelog-master.yaml